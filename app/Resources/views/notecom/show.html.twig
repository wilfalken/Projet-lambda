{% extends 'base.html.twig' %}

{% block body %}
    <h1>Note utilisateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Note</th>
                <td>{{ notecom.note }}</td>
            </tr>
            <tr>
                <th>Id commentaire</th>
                <td>{{ notecom.idcommentaire }}</td>
            </tr>
            <tr>
                <th>Date d'ajout</th>
                <td>{% if notecom.dateajout %}{{ notecom.dateajout|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('notecommentaire_index') }}">Retouner à la liste</a>
        </li>
        <li>
            <a href="{{ path('notecommentaire_edit', { 'id': notecom.idcommentaire }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Effacer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
