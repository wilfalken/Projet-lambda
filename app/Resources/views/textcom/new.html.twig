{% extends 'base.html.twig' %}

{% block body %}
    <h1>Textcomuser creation</h1>
<div class="form-group2">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('textcommentaire_index') }}">Retourner à la liste</a>
        </li>
    </ul>
</div>
{% endblock %}
