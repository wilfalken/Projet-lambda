{% extends 'base.html.twig' %}

{% block body %}
    <h1>Textcomuser</h1>

    <table>
        <tbody>
            <tr>
                <th>Commentaire texte</th>
                <td>{{ textcom.textcom }}</td>
            </tr>
            <tr>
                <th>Id commentaire</th>
                <td>{{ textcom.idcommentaire }}</td>
            </tr>
            <tr>
                <th>Date d'ajout</th>
                <td>{% if textcom.dateajout %}{{ textcom.dateajout|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_textecommentaire_index') }}">Retouner à la liste</a>
        </li>
        <li>
            <a href="{{ path('admin_textecommentaire_edit', { 'id': textcom.idcommentaire }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Effacer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
