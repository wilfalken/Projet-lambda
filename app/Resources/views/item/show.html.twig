{% extends '::base.html.twig' %}
{% block title %}Item{% endblock %}
{% block body %}
    <div class="tablecontainer">
        <h1>Item</h1>

        <table  class="table table-condensed table-striped table-hover">
            <tbody>
                <tr>
                    <th>Id d'item</th>
                    <td>{{ item.iditem }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ item.nomitem }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ item.description }}</td>
                </tr>
                <tr>
                    <th>Photo</th>
                    <td>{{ item.photoitem }}</td>
                </tr>
                <tr>
                    <th>valide ?</th>
                    <td>{% if item.isvalide %}Yes{% else %}No{% endif %}</td>
                </tr>
                <tr>
                    <th>Categorie</th>
                    <td>
                        {% for categorie in item.nomcategorie %}
                            {{ categorie.nomcategorie }}
                        {% endfor %}#}
                    </td>
                </tr>
                <tr>
                    <td>
                        {{ dump(vals) }}{{ dump(item.iditem) }}{{ dump(nomprop) }}{#{{ dump(categorie) }}#} {#{{ dump(propriete) }}#}
                        {#                    {% for propriete in item.proprietes %}
                                                {{ propriete.1 }}
                                            {% endfor %}#}
                    </td>
                </tr>
                {#</td>{{ item.nomcategorie }}</td>#}


            </tbody>
        </table>

        <ul>


            {{ form_start(delete_form) }}

            <input class="btn btn-danger" type="submit" value="Supprimer">
            <a class="btn btn-warning" href="{{ path('item_edit', { 'id': item.iditem }) }}">Editer</a>
            {{ form_end(delete_form) }}
            </br></br>
            <a class="btn btn-primary" href="{{ path('item_index') }}">Retout à la liste</a>



        </ul>
    </div>
{% endblock %}
