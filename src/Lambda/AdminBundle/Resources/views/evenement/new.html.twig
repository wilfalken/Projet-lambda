{% extends '::base.html.twig' %}
{% block title %}Création d'évènement{% endblock %}
{% block body %}
    <div class="form-group2">
    <h1>Creation d'un evenement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        </br><input class="btn btn-success" type="submit" value="Créer" />
    {{ form_end(form) }}
    </br>


            <a class="btn btn-primary btn-xs" href="{{ path('admin_evenement_index') }}">Retourner à la liste</a>


    </div>
{% endblock %}
