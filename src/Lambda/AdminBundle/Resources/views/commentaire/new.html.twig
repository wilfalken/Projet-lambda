{% extends '::base.html.twig' %}
{% block title %}Création de commentaire{% endblock %}
{% block body %}
    <div class="form-signin">
    <h1>Creation de Commentaire</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="Créer" />
    {{ form_end(form) }}
    </br>
    <ul>
      
            <a class="btn btn-primary btn-xs" href="{{ path('commentaire_index') }}">Retour à la liste</a>
      
    </ul>
    </div>
{% endblock %}
